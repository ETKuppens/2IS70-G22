package com.example.cardhub.user_profile;

import android.widget.ImageView;

import org.junit.Test;

/**
 * Tests for the Profile are exclusively done in the DataTests, since that is where the
 * actual functionality of the Profile is. The State is really just an intermediary.
 */
public class ProfileStateTest {

}
